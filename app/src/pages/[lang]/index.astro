---
import NavBar from '@components/ui/organisms/NavBar/index'
import { languages } from '@i18n/ui'
import { generateParamsLanguageList } from '@i18n/utils'
import Layout from '@layouts/Layout.astro'
import '@styles/index.css'
import ThemeToggle from '@components/ui/molecules/ThemeToggle'

export function getStaticPaths() {
  return generateParamsLanguageList(languages)
}

const title = 'Groc'
---

<Layout title={title}>
  <NavBar client:only="react" />
  <ThemeToggle client:only="react" />
</Layout>
